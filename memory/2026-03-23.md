# 2026-03-23

## Halo(aliyun) H2→PostgreSQL 迁移收尾（完成态）
- Aliyun Halo 已完成从 **H2** 切换到 **PostgreSQL**（容器通过 `172.17.0.1:5432` 访问主机 PG）。
- 验收：前台可访问（HOME `HTTP 200`）；`/actuator/health` 返回 `UP`（重启后短暂出现一次空响应，属于启动窗口期，稍等即恢复）。

### 备份与定时任务（新增/确认）
- 迁移后 PostgreSQL 冷备（custom format）已生成并上传 R2：
  - `/root/halo-backups/halo2-pg-20260322_234657.dump`
  - `/root/halo-backups/halo2-pg-20260322_235013.dump`
- 新增 PG 备份脚本：`/root/ops/halo_pg_backup.sh`
  - 功能：pg_dump → 上传 R2（路径 `r2halo:openclaw/backups/halo/aliyun`）→ 本地保留 latest 14
  - crontab：`25 3 * * * /root/ops/halo_pg_backup.sh >/root/ops/halo_pg_backup.log 2>&1`
- 新增本地备份保留策略：`/root/ops/halo_backup_retention.sh`
  - 功能：workdir/pg dump 保留 14 份；h2-script 保留 30 份；迁移临时目录保留 30 份
  - crontab：`40 3 * * * /root/ops/halo_backup_retention.sh >/root/ops/halo_backup_retention.log 2>&1`

### 当前本地备份关键文件（示例）
- workdir：`/root/halo-backups/halo2-workdir-*.tar.gz`
- H2 dump：`/root/halo-backups/halo-h2-script-*.sql`
- PG dump：`/root/halo-backups/halo2-pg-*.dump`

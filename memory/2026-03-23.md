
## Halo API 发文接入（进行中）
- 已确认 `https://www.lambda.xin/` 为 Halo（页面 generator: Halo 2.21.10）。
- 已验证 PAT 可访问 `GET /apis/content.halo.run/v1alpha1/posts|categories|tags`，并可创建 Tag/Category/Post（metadata/spec）。
- 发现权限/接口约束：
  - `PATCH /apis/content.halo.run/v1alpha1/posts/{name}` 需要 `Content-Type: application/json-patch+json`（直接 application/json 会 415）。
  - `GET /apis/content.halo.run/v1alpha1/snapshots/{id}/raw` 返回 403 insufficient_scope；`/posts/{name}/draft|content|publish|release` 等也 403，导致目前只能建空 Post，无法写入正文/发布。
- 已创建分类：displayName=公众号，slug=gong-zhong-hao（id: category-eflr430l）。
- 已创建 tag: tech（id: tag-imaknsqq）。

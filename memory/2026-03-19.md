# 2026-03-19

## RSS → 公众号 morning 流程故障记录
- 07:00 自动出稿失败，根因不是写稿，也不是微信/Notion 发布，而是**封面图生成步骤失败**。
- 错误特征：`Image generation blocked or no valid final image`，`HTTP 502`，属于上游图像服务/图像端点异常。
- 已确认：正文中间产物 `wechat-publisher-out/auto/20260319/article_raw.md` 已生成，说明 top1 选题与 HUGO 写稿成功；失败点发生在调用图像端点生成封面之后，因此后续裁图、组装、推微信草稿箱、同步 Notion 都未执行。
- 稳定性结论：当前主流程存在单点依赖——**封面图失败会阻塞整条发布链路**。
- 后续应做的降级策略：当封面图生成失败时，自动改用默认无文字封面/通用封面，或允许先发布正文再补封面，避免因图像服务抖动导致整条出稿链路挂掉。

## Morning 流程增强与实战验收结果
- 已完成主流程抗阻塞改造：web research 失败可降级到 RSS-only；HUGO 出稿失败可复用已有 raw 稿；封面图失败会走多层 provider fallback，仍失败则改用默认封面；Notion 同步为 best-effort，不阻塞微信草稿箱发布。
- 文生图 fallback 已接入并实测可用的免费模型：
  - SiliconFlow：`Kwai-Kolors/Kolors`
  - BigModel/ZAI：`cogview-3-flash`
- 2026-03-19 07:39 左右做过完整 morning 流程验收，结果成功：
  - 微信草稿箱发布成功
  - Notion 同步成功
  - 结构化状态：`web_research_status=ok`、`article_status=generated`、`cover_status=default`、`notion_status=ok`
- 关键结论：即使封面图生成链未得到最终可用图，本流程也会自动回退到默认封面并继续完成微信草稿箱与 Notion 同步，说明“单点小故障不再阻塞整条 morning 出稿链”。
- Notion 时间字段修正：`scripts/markdown_to_notion_page.py` 现已明确使用 `Asia/Shanghai` 写入 Notion 的“时间”属性，避免出现时区偏差或无时区时间戳。

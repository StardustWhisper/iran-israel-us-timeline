# 2026-03-04

- 01:34 CST 收到 cron 配置：`shared-bbs-check` 每分钟运行 `scripts/shared_bbs_bot.py`（Notion API-only）。
  - 规则：@MOSS 必回；不明白先问清楚；每次发言需 @ 相关节点并署名 `MOSS (a11)`；避免循环（自己的回复不得包含 `@MOSS`）。
  - 通知：仅 stdout==`WROTE` 才触发 Telegram announce；否则必须完全无输出。
  - 仅允许写 Shared/MOSS；ROCK/Spark 数据库只读。

- 14:00-15:00 CST 会话连续性修复：
  - 用户反馈“旧 session 不回复”，确认并非 Telegram 通道整体故障。
  - 执行修复：`openclaw doctor --repair`、`openclaw sessions cleanup --enforce --fix-missing`、`openclaw gateway restart`。
  - 结果：session store 从 1142 降到 500；gateway 重启后运行正常，Telegram 发送正常。
  - 旧会话可恢复性：`telegram:slash:5966032490` 仅在 `sessions.json.bak` 中可见，原 transcript 文件缺失，无法无损切回；可做摘要继承。
